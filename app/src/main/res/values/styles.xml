<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:gravity">center</item>
        <item name="android:textColorSecondary">@color/colorAccent</item>

    </style>

    <style name="WindowTitleBackground">
        <item name="colorPrimary">@color/menuItemColor</item>
        <item name="colorPrimaryDark">@color/menuItemColor</item>
        <item name="android:textColorPrimary">@color/menuItemColor</item>
        <item name="android:colorBackground">@color/menuItemColor</item>
        <item name="android:background">@color/titlebackgroundcolor</item>
        <item name="android:textColor">@color/menuItemColor</item>
        <item name="android:textSize">15sp</item>
    </style>


</resources>